<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- API endpoints: HTTP Basic authentication, CSRF disabled -->
    <security:http pattern="/api/**" use-expressions="true" create-session="stateless">
        <security:csrf disabled="true" />
        <security:http-basic />
        <security:intercept-url pattern="/api/health" access="permitAll" />
        <security:intercept-url pattern="/api/tokens/**" access="hasRole('ADMIN')" />
        <security:intercept-url pattern="/api/validate" access="isAuthenticated()" />
        <security:intercept-url pattern="/api/user" access="isAuthenticated()" />
        <security:intercept-url pattern="/api/**" access="isAuthenticated()" />
    </security:http>

    <!-- Web endpoints: Form-based login -->
    <security:http use-expressions="true">
        <security:intercept-url pattern="/login" access="permitAll" />
        <security:intercept-url pattern="/resources/**" access="permitAll" />
        <security:intercept-url pattern="/**" access="isAuthenticated()" />

        <security:form-login
                login-page="/login"
                login-processing-url="/perform_login"
                default-target-url="/dashboard"
                authentication-failure-url="/login?error=true"
                username-parameter="username"
                password-parameter="password" />

        <security:logout
                logout-url="/perform_logout"
                logout-success-url="/login?logout=true"
                delete-cookies="JSESSIONID" />
    </security:http>

    <!-- In-Memory User Store (legacy-style hardcoded users) -->
    <security:authentication-manager>
        <security:authentication-provider>
            <security:user-service>
                <security:user name="admin" password="{noop}admin123"
                               authorities="ROLE_ADMIN,ROLE_USER" />
                <security:user name="user" password="{noop}user123"
                               authorities="ROLE_USER" />
            </security:user-service>
        </security:authentication-provider>
    </security:authentication-manager>

</beans>
